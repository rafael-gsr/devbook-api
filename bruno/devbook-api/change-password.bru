meta {
  name: change-password
  type: http
  seq: 9
}

post {
  url: {{base-url}}/users/change-password
  body: json
  auth: inherit
}

body:json {
  {
    "new": "newPassword",
    "old": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
