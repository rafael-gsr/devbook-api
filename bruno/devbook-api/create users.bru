meta {
  name: create users
  type: http
  seq: 2
}

post {
  url: {{base-url}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "name": "test",
    "nick": "somenick",
    "email": "example@email.com",
    "password": "secret"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
