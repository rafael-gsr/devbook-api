meta {
  name: login
  type: http
  seq: 3
}

post {
  url: {{base-url}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "example@email.com",
    "password": "secret"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
